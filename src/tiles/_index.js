require("./parallax/parallax.min");

import textClamp from "./text-clamp/text-clamp";
textClamp().init();

import "./video-tile/video-tile";

import swiperCustom from "./slider/slider";
swiperCustom().init();

// require ('./validation/validation');

import form from "./validation/validation";
form().init();